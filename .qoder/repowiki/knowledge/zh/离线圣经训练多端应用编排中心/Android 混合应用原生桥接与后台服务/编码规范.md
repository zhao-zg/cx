- 原生插件类使用 `@CapacitorPlugin` 注解定义名称，并在 `MainActivity` 的 `super.onCreate()` 之前通过 `registerPlugin()` 手动注册。
- 前台服务通过 `Intent` 动作（如 `ACTION_SPEAK`, `ACTION_STOP`）与插件通信，并使用静态 `Listener` 接口回调 JS 层以传递进度或完成状态。
- 崩溃日志通过 `CrashReporter` 捕获并写入应用私有目录的 `crash_log.txt`，避免存储权限问题并供下次启动时读取。