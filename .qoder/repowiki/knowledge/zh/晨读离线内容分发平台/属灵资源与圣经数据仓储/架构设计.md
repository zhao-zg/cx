- 采用扁平化文件存储架构，按内容类型划分为 `bible`（HTML章节）、`bible-db`（SQLite索引）及 `历史合辑`（纯文本档案）三个物理子目录。
- 资源组织遵循时间维度（如 `2025-04`）与逻辑分类（如 `bible`）并行的策略，通过标准化的四位数字编号（如 `0001.html`）实现圣经章节的有序映射。
- 模块内无业务逻辑代码，完全依赖文件系统路径作为数据访问接口，由外部服务层直接读取文件或查询 `CG.db` 获取结构化数据。