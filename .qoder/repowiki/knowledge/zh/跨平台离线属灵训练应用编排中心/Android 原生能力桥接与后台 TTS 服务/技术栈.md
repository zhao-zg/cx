- **Capacitor 6**：跨平台运行时，配置见 `capacitor.config.json`。
- **Android Foreground Service**：用于 TTS 朗读，类型声明为 `mediaPlayback`。
- **TextToSpeech & MediaPlayer**：原生 TTS 引擎结合 `synthesizeToFile` 与 `MediaPlayer` 实现变速不变调播放。