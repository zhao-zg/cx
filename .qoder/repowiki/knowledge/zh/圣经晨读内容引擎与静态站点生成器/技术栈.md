- **构建工具**: Python 3 + `python-docx` (Word 解析) + `Jinja2` (模板渲染)。
- **前端核心**: 原生 JavaScript (ES6+) + Web Components 思想 (模块化 JS)。
- **存储与离线**: Service Workers (Cache API) + IndexedDB (`localforage`) + JSON 数据持久化。