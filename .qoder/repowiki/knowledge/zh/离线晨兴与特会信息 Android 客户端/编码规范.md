- 在 `super.onCreate()` 之前完成所有 Capacitor 插件的注册以及全局异常处理器的安装。
- 使用 `@CapacitorPlugin` 注解定义插件类，并通过 `PluginMethod.RETURN_PROMISE` 支持异步调用。
- 通过 `Intent` 动作（如 `ACTION_SPEAK`）驱动前台服务状态流转，并利用静态回调接口实现服务向插件的事件回传。