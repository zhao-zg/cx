- **Capacitor 6**：跨平台运行时，配置见 `capacitor.config.json`。
- **Android Foreground Service**：用于 `TTSForegroundService`，确保息屏后语音不中断，依赖 `FOREGROUND_SERVICE_MEDIA_PLAYBACK` 权限。
- **LocalForage & Cache API**：用于离线数据存储与静态资源拦截。