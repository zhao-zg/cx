- **构建流程**：需先执行 `npx cap sync android` 同步 Web 资产与插件配置，再使用 Android Studio 或 `./gradlew assembleRelease` 编译 APK。
- **资源打包**：大量按年月分类的训练数据（`assets/public/1935-01` 至 `2026-02`）需在构建前预置，或通过 `resource-packs` 动态下载。