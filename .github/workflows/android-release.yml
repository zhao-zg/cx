name: 构建安卓APK

on:
  push:
    tags:
      - 'v*.*.*'  # 只在推送版本号标签时触发，如 v1.0.0, v1.2.3
  workflow_dispatch:  # 允许手动触发
    inputs:
      sign_apk:
        description: '是否签名APK'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

permissions:
  contents: write  # 允许创建 Release 和上传文件

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      
    - name: 设置 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        
    - name: 设置 Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: 设置 Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: 安装 LibreOffice
      run: |
        sudo apt-get update
        sudo apt-get install -y libreoffice
        
    - name: 安装 Python 依赖
      run: |
        pip install -r requirements.txt
        
    - name: 安装 Node.js 依赖
      run: npm install
      
    - name: 创建应用启动页
      run: |
        mkdir -p output
        
        # 提取版本号
        VERSION=${GITHUB_REF#refs/tags/v}
        if [ -z "$VERSION" ]; then
          VERSION="0.1.0"
        fi
        
        # 替换版本号并复制
        sed "s/CURRENT_APK_VERSION: '0.1.0'/CURRENT_APK_VERSION: '$VERSION'/g" \
          src/templates/app_index.html > output/index.html
        
        # 复制静态图标
        mkdir -p output/icons
        cp src/static/icons/icon.svg output/icons/
        
        # 创建 manifest.json（使用静态图标）
        cat > output/manifest.json << 'EOF'
        {
          "name": "TeHui",
          "short_name": "TeHui",
          "start_url": "./index.html",
          "display": "standalone",
          "background_color": "#667eea",
          "theme_color": "#667eea",
          "icons": [
            {
              "src": "./icons/icon.svg",
              "sizes": "any",
              "type": "image/svg+xml",
              "purpose": "any maskable"
            }
          ]
        }
        EOF
        
        echo "✓ 应用启动页已创建"
        ls -lh output/
      
    - name: 添加安卓平台
      run: |
        npx cap add android
        
        # 确保 android 目录存在
        if [ ! -d "android" ]; then
          echo "✗ Android 平台添加失败"
          exit 1
        fi
        echo "✓ Android 平台添加成功"
      
    - name: 配置安卓项目版本信息
      run: |
        # 提取版本号
        VERSION=${GITHUB_REF#refs/tags/v}
        if [ -z "$VERSION" ]; then
          VERSION="0.1.0"
        fi
        
        # 计算 versionCode (将 1.0.0 转换为 10000)
        IFS='.' read -ra PARTS <<< "$VERSION"
        VERSION_CODE=$((${PARTS[0]} * 10000 + ${PARTS[1]:-0} * 100 + ${PARTS[2]:-0}))
        
        # 创建 variables.gradle
        cat > android/variables.gradle << 'EOF'
        ext {
            minSdkVersion = 22
            compileSdkVersion = 34
            targetSdkVersion = 34
            androidxActivityVersion = '1.8.0'
            androidxAppCompatVersion = '1.6.1'
            androidxCoordinatorLayoutVersion = '1.2.0'
            androidxCoreVersion = '1.12.0'
            androidxFragmentVersion = '1.6.2'
            coreSplashScreenVersion = '1.0.1'
            androidxWebkitVersion = '1.9.0'
            junitVersion = '4.13.2'
            androidxJunitVersion = '1.1.5'
            androidxEspressoCoreVersion = '3.5.1'
            cordovaAndroidVersion = '10.1.1'
        }
        EOF
        
        # 确保 build.gradle 中有正确的版本信息
        if grep -q "versionCode" android/app/build.gradle; then
          sed -i "s/versionCode [0-9]*/versionCode $VERSION_CODE/" android/app/build.gradle
          sed -i "s/versionName \"[^\"]*\"/versionName \"$VERSION\"/" android/app/build.gradle
        else
          # 如果没有版本信息，添加到 defaultConfig
          sed -i "/defaultConfig {/a\        versionCode $VERSION_CODE\n        versionName \"$VERSION\"" android/app/build.gradle
        fi
        
        # 确保 namespace 存在（Android Gradle Plugin 7.0+ 需要）
        if ! grep -q "namespace" android/app/build.gradle; then
          sed -i "/android {/a\    namespace 'com.tehui.app'" android/app/build.gradle
        fi
        
        echo "✓ 版本配置完成: versionCode=$VERSION_CODE, versionName=$VERSION"
        
        # 显示配置结果
        echo "=== build.gradle 版本信息 ==="
        grep -A 2 "versionCode\|versionName\|namespace" android/app/build.gradle || echo "未找到版本信息"
      
    - name: 同步 Capacitor
      run: |
        echo "=== 同步 Capacitor ==="
        npx cap sync android
        echo "✓ 同步完成"
        
        # 显示生成的文件
        echo ""
        echo "=== Android 项目结构 ==="
        ls -la android/
        echo ""
        echo "=== AndroidManifest.xml 内容 ==="
        cat android/app/src/main/AndroidManifest.xml
      
    - name: 创建固定签名密钥
      run: |
        echo "=== 使用固定签名密钥 ==="
        
        # 使用固定的 base64 编码密钥（已预先生成）
        # 密钥信息：
        # - Alias: tehui
        # - Password: tehui2024
        # - Validity: 10000 days
        # 这样每次构建使用相同的密钥，可以覆盖安装
        cat > my-release-key.keystore.b64 << 'EOF'
        
        EOF
        
        # 解码密钥文件
        base64 -d my-release-key.keystore.b64 > my-release-key.keystore
        
        echo "✓ 固定密钥已创建"
        
    - name: 配置签名
      run: |
        echo "=== 配置 APK 签名 ==="
        
        # 创建签名配置文件
        cat > android/keystore.properties << EOF
        storeFile=my-release-key.keystore
        storePassword=tehui2024
        keyAlias=tehui
        keyPassword=tehui2024
        EOF
        
        # 复制密钥文件到 android 目录
        cp my-release-key.keystore android/
        
        # 创建 Python 脚本来插入签名配置
        cat > insert_signing.py << 'EOF'
        import re
        
        with open('android/app/build.gradle', 'r') as f:
            content = f.read()
        
        # 在 android { 后插入 signingConfigs
        signing_config = "\n    signingConfigs {\n        release {\n            storeFile file(\"../my-release-key.keystore\")\n            storePassword \"tehui2024\"\n            keyAlias \"tehui\"\n            keyPassword \"tehui2024\"\n        }\n    }\n"
        content = re.sub(r'(android\s*\{)', r'\1' + signing_config, content, count=1)
        
        # 在 buildTypes.release { 后插入 signingConfig
        content = re.sub(r'(buildTypes\s*\{[^}]*release\s*\{)', r'\1\n            signingConfig signingConfigs.release', content, count=1)
        
        with open('android/app/build.gradle', 'w') as f:
            f.write(content)
        
        print("✓ 签名配置已插入")
        EOF
        
        python3 insert_signing.py
        
        echo "✓ 签名配置完成"
        echo ""
        echo "=== 验证密钥文件 ==="
        ls -lh android/my-release-key.keystore
        echo ""
        echo "=== build.gradle 开头部分（查找 android 块）==="
        head -50 android/app/build.gradle | grep -A 10 "android"
        echo ""
        echo "=== build.gradle 签名部分 ==="
        grep -A 20 "signingConfigs" android/app/build.gradle || echo "未找到 signingConfigs"
      
    - name: 授予 Gradle 执行权限
      run: chmod +x android/gradlew
        
    - name: 构建 APK
      run: |
        echo "=== 构建 APK ==="
        cd android
        
        # 设置 Gradle 属性，增加下载超时时间
        mkdir -p ~/.gradle
        cat > ~/.gradle/gradle.properties << 'EOF'
        org.gradle.daemon=false
        org.gradle.parallel=true
        org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m
        systemProp.http.connectionTimeout=60000
        systemProp.http.socketTimeout=60000
        EOF
        
        # 构建 APK，添加重试机制
        MAX_RETRIES=1
        RETRY_COUNT=0
        
        while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
          echo "尝试构建 APK (第 $((RETRY_COUNT + 1)) 次)..."
          
          if ./gradlew clean assembleRelease --stacktrace --no-daemon; then
            echo "✓ APK 构建成功"
            cd ..
            exit 0
          else
            RETRY_COUNT=$((RETRY_COUNT + 1))
            if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
              echo "⚠️ 构建失败，30秒后重试..."
              sleep 30
            fi
          fi
        done
        
        echo "✗ APK 构建失败，已重试 $MAX_RETRIES 次"
        cd ..
        exit 1
        
    - name: 验证和重命名 APK
      run: |
        VERSION=${GITHUB_REF#refs/tags/v}
        if [ -z "$VERSION" ]; then
          VERSION=$(date +%Y%m%d-%H%M%S)
        fi
        
        echo "=== 构建的 APK ==="
        ls -lh android/app/build/outputs/apk/release/
        
        if [ -z "$(ls -A android/app/build/outputs/apk/release/*.apk 2>/dev/null)" ]; then
          echo "✗ 没有找到 APK 文件"
          exit 1
        fi
        
        cd android/app/build/outputs/apk/release/
        for apk in *.apk; do
          if [ -f "$apk" ]; then
            NEW_NAME="TeHui-v${VERSION}.apk"
            cp "$apk" "../../../../../../$NEW_NAME"
            echo "✓ APK: $NEW_NAME"
            echo "  大小: $(du -h "$apk" | cut -f1)"
            echo "  MD5: $(md5sum "$apk" | cut -d' ' -f1)"
            
            # 使用 aapt 验证
            echo ""
            echo "=== APK 信息 ==="
            $ANDROID_HOME/build-tools/*/aapt dump badging "$apk" | head -10
            break
          fi
        done
        cd ../../../../../../
        
        echo ""
        echo "=== 最终 APK ==="
        ls -lh TeHui-*.apk
        
    - name: 上传 APK 到 Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: TeHui-*.apk
        
    - name: 创建 Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: |
          TeHui-*.apk
        body: |
          ## �  特会信息 ${{ github.ref_name }}
          
          ### 📦 下载说明
          
          #### APK 包（约 3-5 MB）
          - 文件名：`TeHui-${{ github.ref_name }}.apk`
          - 特点：轻量级应用壳，从服务器动态加载内容
          - 功能：
            - 支持多服务器切换（长按 Logo）
            - 自动检查更新（点击 Logo 10 次）
            - PWA 缓存，支持离线浏览
          - 要求：安卓 5.1 或更高版本（API 22+）
          
          ### ⚠️ 安装提示
          
          如果安装时提示"解析包出现问题"：
          1. 确认下载完整（检查文件大小）
          2. 清除旧版本后重新安装
          3. 确保安卓版本 ≥ 5.1（API 22）
          4. 尝试在设置中清除下载管理器的缓存
          5. 检查是否启用了"未知来源"安装权限
          
          ### 📝 版本信息
          - 版本号: ${{ github.ref_name }}
          - 包名: com.tehui.app
          - 签名状态: ${{ github.event.inputs.sign_apk == 'true' && '✅ 已签名' || '⚠️ 未签名（测试版）' }}
          - 构建时间: ${{ github.event.head_commit.timestamp }}
          
          ### 🚀 使用说明
          
          1. 下载并安装 APK
          2. 打开应用，自动从服务器加载内容
          3. 长按 Logo 可切换服务器
          4. 点击 Logo 10 次可检查 APK 更新
          5. 内容会自动缓存，支持离线浏览
          
          ### 💡 提示
          - 这是测试版本，用于验证基础功能
          - 如果安装成功，后续版本会添加离线版支持
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
