name: Auto Download Resources

on:
  schedule:
    # 每天北京时间早上 9 点运行 (UTC 1:00)
    - cron: '0 1 * * *'
  workflow_dispatch: # 允许手动触发

permissions:
  contents: write  # 允许推送代码

jobs:
  download-resources:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install chromium
          playwright install-deps chromium

      - name: Download resources
        run: |
          python down_resource.py
        continue-on-error: false

      - name: Check for changes
        id: check_changes
        run: |
          git add resource/
          if git diff --staged --quiet; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "ℹ️ 没有新资源需要下载"
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "✅ 发现新资源"
            git diff --staged --stat
          fi

      - name: Commit and push changes
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git commit -m "🤖 自动下载: 新增训练资源 $(date +'%Y-%m-%d %H:%M')"
          git push

      - name: Get version
        if: steps.check_changes.outputs.has_changes == 'true'
        id: get_version
        run: |
          # 获取最新的 tag
          LATEST_TAG=$(git tag -l "v*.*.*" | sort -V | tail -n 1)
          if [ -z "$LATEST_TAG" ]; then
            # 如果没有 tag，从 v1.0.0 开始
            NEW_VERSION="v1.0.0"
          else
            # 解析版本号并递增 patch 版本
            VERSION=${LATEST_TAG#v}
            MAJOR=$(echo $VERSION | cut -d. -f1)
            MINOR=$(echo $VERSION | cut -d. -f2)
            PATCH=$(echo $VERSION | cut -d. -f3)
            PATCH=$((PATCH + 1))
            NEW_VERSION="v${MAJOR}.${MINOR}.${PATCH}"
          fi
          echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "新版本: $NEW_VERSION"

      - name: Create tag
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git tag ${{ steps.get_version.outputs.version }}
          git push origin ${{ steps.get_version.outputs.version }}

      - name: Create GitHub Release
        if: steps.check_changes.outputs.has_changes == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.get_version.outputs.version }}
          name: 训练资源 ${{ steps.get_version.outputs.version }}
          body: |
            ## 📦 自动下载的训练资源
            
            **下载时间**: $(date +'%Y-%m-%d %H:%M:%S')
            **版本号**: ${{ steps.get_version.outputs.version }}
            
            ### 📂 包含内容
            - 新增的训练Word文档（听抄、晨兴、经文）
            
            ### 📝 说明
            此版本仅包含原始Word文档资源，HTML页面需要通过部署工作流生成。
            
            > 此版本由 GitHub Actions 自动生成
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Summary
        if: always()
        run: |
          if [ "${{ steps.check_changes.outputs.has_changes }}" == "true" ]; then
            echo "✅ 成功下载新资源并创建Release"
            echo "📦 Release: ${{ steps.get_version.outputs.version }}"
            echo "📝 HTML生成将由部署工作流处理"
          else
            echo "ℹ️ 没有新资源需要更新"
          fi
