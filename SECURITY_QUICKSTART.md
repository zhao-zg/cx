# 🚀 安全构建快速指南

## 立即开始

### 1️⃣ 安装依赖

```bash
# Python 加密库
pip install cryptography

# Node.js 混淆工具
npm install
```

### 2️⃣ 开发模式

```bash
# 正常构建（不混淆，开启调试）
npm run build

# 预览
npm run android:dev
```

### 3️⃣ 生产打包

```bash
# 🔐 安全构建（自动混淆+加密+优化）
npm run build:secure

# 同步并打包 APK
npm run cap:sync
cd android && ./gradlew assembleRelease

# ⚠️ 重要：恢复开发配置
npm run restore:dev
```

---

## 🎯 一键命令

```bash
# 开发
npm run android:dev

# 生产（带安全保护）
npm run android:build:secure && npm run restore:dev
```

---

## ✅ 已实施的安全措施

1. **关闭调试模式** - 禁用远程调试
2. **代码混淆** - JS 代码完全混淆
3. **内容加密** - 敏感内容加密存储

---

## 📖 详细文档

查看 [SECURITY.md](SECURITY.md) 了解完整说明。

---

## ⚠️ 重要提醒

**生产打包后务必运行**：
```bash
npm run restore:dev
```

否则会影响本地开发调试！
